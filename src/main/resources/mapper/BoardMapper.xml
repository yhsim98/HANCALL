<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.BoardMapper">

    <insert parameterType="domain.Board" id="insertBoard">
        INSERT INTO board(writer_id, starting_point, destination, title, content, created_date)
            VALUES(#{writerId}, #{startingPoint}, #{destination}, #{title}, #{content}, DEFAULT)
    </insert>
</mapper>